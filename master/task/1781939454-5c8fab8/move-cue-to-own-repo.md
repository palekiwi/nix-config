---
status: complete
priority: normal
title: move cue to own repo
---

In `774c81b` we extracted `cue.lua` into a basic plugin
that is hosted locally in this repo.

Let's move it into a newly created GH repository as a full-fledged
independent nvim plugin.

plugin repo dir: `/home/pl/code/palekiwi-labs/cue.nvim`
