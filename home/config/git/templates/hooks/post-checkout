#!/usr/bin/env bash
# nix-config managed: refresh via git-hooks-sync

# Only run if we're checking out a branch (not individual files)
if [ "$3" = "1" ]; then
    command -v git-pr-sync >/dev/null 2>&1 && git-pr-sync
fi

# Hook safety: never fail the checkout.
exit 0
